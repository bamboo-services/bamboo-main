-- --------------------------------------------------------------------------------
-- 移除 bm_link_color.type 列（冗余字段）
-- --------------------------------------------------------------------------------
-- 用途：炫彩身份已由保留 ID=1（constants.BuiltinFancyColorID）承载，内置炫彩不落库，
--       落库颜色均为普通配色，type 恒为 0，列本身无信息量。此处清理存量库遗留列。
--
-- 注意：GORM AutoMigrate 只会新增列、不会删除已有列，故需手动执行此脚本清理。
--       新环境由 AutoMigrate 直接建无 type 列的表，无需执行。
-- --------------------------------------------------------------------------------

ALTER TABLE bm_link_color DROP COLUMN IF EXISTS type;
